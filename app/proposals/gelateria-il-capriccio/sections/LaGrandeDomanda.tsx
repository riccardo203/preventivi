export function LaGrandeDomanda() {
  return (
    <section
      className="h-screen bg-white flex items-center justify-center px-16"
      style={{ scrollSnapAlign: "start", scrollSnapStop: "always" }}
    >
      <p className="heading-display text-center max-w-2xl">
        Se tutto il quartiere vi vedesse più volte al giorno, tutti i giorni, tutto l'anno... cosa succederebbe?
      </p>
    </section>
  )
}
